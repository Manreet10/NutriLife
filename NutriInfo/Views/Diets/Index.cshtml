@model IEnumerable<NutriInfo.Models.Diet>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Benefit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DifficultLevel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Benefit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DifficultLevel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DietId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DietId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DietId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
